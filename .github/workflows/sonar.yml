name: SonarQube Scan

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  sonarqube:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v2
        env:
          SONAR_TOKEN: ${e6e99cbd10268fe45773ca62358ffdec51854416}
          SONAR_HOST_URL: ${https://sonarcloud.io/}
